<h1>Productos</h1>
{{#each products}}
  <div>
    <h2>{{this.title}}</h2>
    <p>{{this.description}}</p>
    <p>Precio: ${{this.price}}</p>
    <button>Agregar al carrito</button>
  </div>
{{/each}}
    